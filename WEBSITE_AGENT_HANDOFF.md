# 微信读书助手网站版 Agent 交接说明

## 1. 产品是什么

这是一个以“阅读上下文”为核心的个人书籍管理工具。它不只记录“我有哪些书”，还记录：

- 我为什么读这本书。
- 我带着什么问题阅读。
- 这本书需要多少认知精力。
- 哪本书把我带到这里，这本书又把我带向哪里。
- 一本书属于哪些长期阅读主题。

当前产品是运行在微信读书书架页的 Tampermonkey 油猴脚本。网站版应把“本地完整书库”作为核心，不依赖微信读书页面 DOM，也不应把微信读书书架当作唯一数据源。

## 2. 核心产品原则

1. **本地优先**：阅读数据先保存在用户设备中，云同步是可选能力。
2. **外部书是一等书籍**：手动添加的书与微信读书书籍使用同一种主档，可加入主题组、维护上下文、分级和建立关系。
3. **稳定 ID 优先于书名**：同名书可以是不同版本，不能按书名静默合并。
4. **阅读关系是独立数据**：关系不是某本书描述中的一段文本，而是连接两本书的有向记录。
5. **快照不能覆盖主档**：关系和笔记中的书籍快照仅用于兼容和显示，书籍主档才是权威来源。
6. **迁移和同步必须幂等**：读取、规范化或展示数据不得反复制造 `updatedAt` 变化，否则会触发无限同步循环。

## 3. 当前已有功能

### 3.1 完整书库

- 自动从微信读书网页书架扫描并更新书籍。
- 手动创建外部书籍。
- 书籍字段包括书名、作者、封面 URL、详情 URL和阅读入口 URL。
- 搜索书名或作者。
- 按“全部、微信读书、外部书、待关联”筛选。
- 打开阅读上下文、编辑主档、删除书籍。
- 仍存在于微信读书书架中的书禁止从本地主档删除。
- 删除本地主档前显示受影响的主题组、上下文、分级和关系数量，确认后级联删除。

### 3.2 微信读书版本关联

- 当外部书与新扫描到的微信读书书籍同名时，形成“待关联”候选，不自动合并。
- 用户可以确认关联，或标记“不是同一本”。
- 确认后保留外部书原本的稳定 ID，补充微信读书 ID、封面和阅读入口。
- 主题组成员关系取并集。
- 阅读关系重定向到保留的书籍 ID。
- 上下文、阅读问题和阅读分级发生冲突时由用户逐项选择。

### 3.3 主题组

- 创建、编辑和删除主题组。
- 主题组包含名称、描述和书籍 ID 列表。
- 一本书可以属于主题组，并保留自己的独立上下文和关系。
- 书籍可以在单个主题组内置顶；多个置顶按 `pinnedBookIds` 顺序显示，新置顶的书排在最前面。
- 组卡片显示“有上下文书籍数 / 组内总书数”。
- 只要“为什么读这本书”或“阅读问题”任一不为空，就视为有上下文。
- “移出主题组”只解除成员关系，不删除书籍主档。

### 3.4 单本书阅读上下文

上下文弹窗目前包含：

1. 书籍信息。
2. 阅读脉络，仅存在入边时显示。
3. “我为什么读这本书”。
4. “阅读问题”。
5. 阅读强度选择。
6. 下一站，仅存在出边时显示。
7. 删除描述、添加发现、查看关系图和保存等操作。

当前已经移除了“阅读状态 / 动机”模块，不应在网站版重新引入。

点击主题组、分级阅读、书籍管理或关系卡片中的书籍，首先打开上下文，而不是直接跳转阅读页面。上下文中只有存在 `readerUrl` 时才显示“阅读”，仅有 `detailUrl` 时显示“查看详情”。

### 3.5 分级阅读

用于适应不同精力状态：

- `deep`：深度阅读。
- `light`：轻度阅读。
- `casual`：随便读读。
- 未分级：没有对应分级记录，是默认状态。

页面显示各类别数量，支持搜索和筛选。分级表达的是阅读所需认知资源，不是阅读进度。

### 3.6 阅读路径

每条关系连接两本正式书籍，方向为：

```text
fromBookId -> toBookId
前一本书       后一本书
```

- 对当前书而言，指向它的关系显示为“阅读脉络”。
- 从当前书指出的关系显示为“下一站”。
- 关系类型包括延伸阅读、作者引用和问题驱动。
- 每条关系记录“为什么想继续”。
- 同一方向的两本书只有一条关系；反向关系独立存在，允许循环。
- 禁止一本书关联到自己。
- 关系卡片支持打开书籍上下文、编辑关系和删除关系。
- 关系原因默认最多显示三行，悬停可查看完整内容。

### 3.7 关系图

当前使用 Cytoscape.js `3.34.2`，提供三个范围：

- 当前书：显示当前书及全部一跳入边和出边。
- 主题组：显示组内参与关系的书，并包含直接相连的一跳组外节点。
- 全库：显示所有参与关系的节点，不显示没有关系的孤立书籍。

关系图支持搜索、关系类型筛选、适应画布、重新布局、全屏、节点选择和边详情。无环图使用有向 breadth-first 布局，存在循环时使用 CoSE。

### 3.8 Obsidian WHY 匹配

- 当前脚本可通过 Cloudflare Worker 读取 Obsidian 书目匹配结果。
- Obsidian 中的 WHY 上下文在脚本中视为权威只读内容。
- 本地仍可保存阅读分级。
- 当前导出文件不包含 Obsidian 匹配缓存，因此网站版导入导出文件后只能读取本地 `notes`，除非另行实现 Obsidian 集成。

### 3.9 Cloudflare KV 同步

- 使用现有 `/api/v2` Worker 接口和 Cloudflare KV，不使用 D1。
- 书籍、主题组、上下文、分级和关系分别同步。
- 删除通过 tombstone 同步。
- 合并依据 `updatedAt` 和稳定冲突裁决。
- 旧客户端缺少新字段时不能被解释为“删除云端数据”。
- 展示、读取、封面代理和数据迁移不能更新业务记录时间戳，除非数据确实发生一次持久变更。

### 3.10 数据导出

“书籍管理”页可以下载：

```text
weread-library-YYYY-MM-DD-HHmmss.json
```

导出包含：

- `books`：完整书籍主档。
- `groups`：主题组和主题描述。
- `notes`：为什么读与阅读问题。
- `readingLevels`：阅读分级。
- `relations`：阅读关系。

导出不包含云同步凭据、设备 ID、同步状态、tombstone、Obsidian 缓存和临时 UI 状态。

完整字段说明见 [`DATA_EXPORT_FORMAT.md`](./DATA_EXPORT_FORMAT.md)，机器校验见 [`weread-library-export.schema.json`](./weread-library-export.schema.json)。

## 4. 数据关系

网站版应先加载 `books`，再解析其他集合：

```text
books[bookId]
   ↑                 ↑          ↑                 ↑
groups             notes   readingLevels      relations
bookIds/pinnedBookIds 对象键     对象键       fromBookId/toBookId
```

权威关系如下：

- `books` 的对象键应等于 `LibraryBook.id`。
- `groups[].bookIds[]` 引用书籍 ID。
- `groups[].pinnedBookIds[]` 是 `bookIds` 的有序子集；字段缺失表示没有置顶书籍。
- `notes` 的对象键是书籍 ID。
- `readingLevels` 的对象键是书籍 ID。
- `relations[].fromBookId` 和 `toBookId` 引用书籍 ID。
- `note.book`、`readingLevel.book`、`relation.from` 和 `relation.to` 都只是快照。

遇到缺失书籍引用时，应保留记录并标记 unresolved，不要按标题自动合并或直接丢弃。

## 5. 网站版建议边界

### 第一阶段

- 导入 `weread-library-*.json`。
- 校验 `format`、`version` 和 JSON Schema。
- 展示完整书库、主题组、阅读上下文、分级和关系图。
- 使用 IndexedDB 保存网站自己的编辑结果。
- 支持再次导出同一业务格式。

### 第二阶段

- 增加书籍、主题组、上下文、分级和关系的完整编辑能力。
- 增加封面代理或缓存。
- 增加删除影响预览与级联清理。

### 第三阶段

- 在确认同步协议兼容后，再接入现有 Cloudflare Worker。
- 同步前必须设计客户端身份、幂等迁移、冲突处理和离线编辑策略。
- 不建议网站第一版直接连接生产 KV，尤其不要把页面初始化或数据规范化当成本地修改上传。

## 6. 封面处理注意事项

微信读书封面 URL 在普通 `<img>` 中通常可以直接显示，但关系图、Canvas 或需要读取像素的场景会受到跨域限制。当前油猴脚本通过特权网络请求把微信读书封面转换为 data URL 后交给 Cytoscape。

网站版部署到其他域名时，不应假设所有微信读书 CDN 地址都长期允许跨域读取。建议使用同源图片代理或服务端缓存，并设置：

- URL 白名单，仅允许预期的微信读书封面域名。
- 响应体大小、内容类型和超时限制。
- 缓存与失败占位封面。
- 禁止代理任意 URL，避免形成 SSRF 或开放代理。

## 7. UI 与交互基线

- 产品是个人阅读管理工具，界面应安静、紧凑、适合反复扫描，不做营销式首页。
- 主要视图应直接进入书库或上次使用的管理页。
- 图标按钮使用 SVG，并提供 tooltip、可访问名称和正确的鼠标指针。
- 长英文书名、长关系原因和描述不能溢出容器。
- 桌面和约 720px 窄屏都应可用。
- 空的“阅读脉络”和“下一站”连标题也不显示，但“添加发现”始终保留。
- 删除、编辑和菜单图标必须视觉居中。
- 阅读入口必须是上下文中的明确按钮，书籍卡片本身优先打开上下文。

## 8. 不应照搬到网站版的部分

- 微信读书 DOM 扫描与原生书架隐藏逻辑只属于油猴脚本。
- `/web/shelf/archive/*` 路由绕过逻辑对独立网站没有意义。
- 依赖 Tampermonkey `GM_xmlhttpRequest` 的封面请求应改成网站自己的受控后端能力。
- 油猴脚本的面板挂载、MutationObserver 和页面事件拦截不属于领域逻辑，应重新实现。

## 9. 现有实现入口

- [`weread-local-topic-shelf.user.js`](./weread-local-topic-shelf.user.js)：现有油猴脚本及全部领域逻辑。
- [`DATA_EXPORT_FORMAT.md`](./DATA_EXPORT_FORMAT.md)：导出格式的人类可读说明。
- [`weread-library-export.schema.json`](./weread-library-export.schema.json)：导出格式的 JSON Schema。

网站 Agent 应把数据模型和行为规则视为可复用契约，把当前 DOM 操作和样式视为参考实现，而不是网站架构模板。
