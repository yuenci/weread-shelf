# 更新记录

## 0.8.0

- 主题卡片上下文提高到 13px、三行预览，可展开全文；书籍标题整理下划线，版本后缀作为标签展示。
- 书籍编辑增加可选的显示书名，保留原始书名用于识别与匹配。
- 主题组、分级阅读和书籍管理卡片提供直接阅读入口，仅在存在阅读链接时显示。
- 增加展开／收起工作区。宽屏中，上下文与关系图在内容区显示，并提供返回入口；切换宽度保留当前表单。
- 主题计数明确标注“上下文”，书架计数标注“当前已识别”，Obsidian 匹配统计明确数据范围。
- 关系图默认从当前主题或当前书进入，可以切换全库；按相连书籍分块排列，保留可读缩放，支持节点相邻路径突出显示。
- 关系文字列表展示完整缘由；书名或缘由搜索保留关系两端，支持叠加关系类型筛选。

### 验证与本地预览

运行 `node --test tests/reading-workspace.test.cjs` 检查用户脚本的回归行为，无需安装测试依赖。

`tests/reading-workspace.html` 是虚构数据预览页，只允许在 localhost 或 127.0.0.1 运行。应将它复制到独立临时目录的 `index.html`，连同根目录用户脚本一起提供本地 HTTP 服务，避免公开仓库内的私人导出文件。页面会覆盖该预览域名下的样例书库，禁止放到真实微信读书域名。

更新油猴中的 `weread-local-topic-shelf.user.js` 后刷新微信读书书架即可使用；本次不改变 Obsidian 插件或云端服务。
